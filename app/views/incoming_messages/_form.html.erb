<%= form_with(model: incoming_message) do |form| %>
  <% if incoming_message.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(incoming_message.errors.count, "error") %> prohibited this incoming_message from being saved:</h2>

      <ul>
        <% incoming_message.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
