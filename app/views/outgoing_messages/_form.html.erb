<%= form_with(model: outgoing_message) do |form| %>
  <% if outgoing_message.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(outgoing_message.errors.count, "error") %> prohibited this outgoing_message from being saved:</h2>

      <ul>
        <% outgoing_message.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :incoming_message_id, style: "display: block" %>
    <%= form.text_field :incoming_message_id %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
